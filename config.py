from pathlib import Path


CURRENT_DIR = Path(__file__).parent

TRANSACTION_PATH_JSON = CURRENT_DIR / "data" / "operations.json"

TRANSACTION_PATH_CSV = CURRENT_DIR / "data" / "transactions.csv"

TRANSACTION_PATH_XLSX = CURRENT_DIR / "data" / "transactions_excel.xlsx"
